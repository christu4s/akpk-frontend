import { Box } from "@mui/material";
import React from "react";

export default function ServiceSelect({ selected, img, setSelected }) {
  return (
    <Box>
      <Box></Box>
      <Box></Box>
    </Box>
  );
}
